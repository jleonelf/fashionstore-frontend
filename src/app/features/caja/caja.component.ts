import {CommonModule} from '@angular/common';import{Component,OnInit}from'@angular/core';import{FormsModule}from'@angular/forms';import{Ciclo2Service}from'../../core/services/ciclo2.service';import{AuthService}from'../../core/services/auth.service';import{ProductoService}from'../../core/services/producto.service';import{Comprobante,ItemVenta,MetodoCaja,Reserva}from'../../core/models/ciclo2.models';import{VarianteDTO}from'../../core/models/catalogo.models';
@Component({selector:'app-caja',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./caja.component.html',styleUrls:['./caja.component.css']})export class CajaComponent implements OnInit{codigo='';reserva?:Reserva;variantes:VarianteDTO[]=[];items:ItemVenta[]=[];variante='';cantidad=1;metodo:MetodoCaja='EFECTIVO';cargando=false;procesando=false;error='';comprobante?:Comprobante;constructor(private api:Ciclo2Service,public auth:AuthService,private productos:ProductoService){}ngOnInit(){this.productos.gestionarVariantes().subscribe(v=>this.variantes=v)}buscar(){if(!this.codigo)return;this.cargando=true;this.error='';this.api.reservaPorCodigo(this.codigo.trim()).subscribe({next:r=>{this.reserva=r;this.items=r.detalles.filter(d=>d.cantidad_reservada>d.cantidad_vendida).map(d=>({detalle_reserva_id:d.id,variante_id:d.variante_id,cantidad:d.cantidad_reservada-d.cantidad_vendida}));this.cargando=false},error:e=>{this.error=this.msg(e);this.cargando=false}})}agregar(){if(!this.variante||this.cantidad<1)return;this.items.push({variante_id:this.variante,cantidad:this.cantidad});this.variante='';this.cantidad=1}quitar(i:number){this.items.splice(i,1)}v(id:string){return this.variantes.find(x=>x.id===id)?.sku||id.slice(0,8)}vender(){const u=this.auth.obtenerUsuarioActual();if(!u?.sucursal_id||!this.items.length||this.procesando)return;this.procesando=true;this.error='';this.api.registrarVenta({reserva_id:this.reserva?.id,sucursal_id:u.sucursal_id,cliente_id:this.reserva?.cliente_id,metodo:this.metodo,items:this.items}).subscribe({next:v=>this.api.comprobante(v.id).subscribe({next:c=>{this.comprobante=c;this.procesando=false;this.items=[]},error:e=>{this.error=this.msg(e);this.procesando=false}}),error:e=>{this.error=this.msg(e);this.procesando=false}})}nueva(){this.reserva=undefined;this.comprobante=undefined;this.codigo='';this.items=[]}imprimir(){window.print()}private msg(e:any){return e?.error?.detail||'No se pudo completar la venta.'}}
